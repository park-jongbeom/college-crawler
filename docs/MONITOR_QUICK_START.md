# ⚡ 모니터링 대시보드 빠른 시작

웹 모니터링 대시보드를 **1분 안에** 시작하는 방법입니다.

## 🚀 운영 서버에서 시작하기

### 1단계: Docker Compose 업데이트

```bash
cd /media/ubuntu/data120g/college-crawler

# 최신 코드 pull
git pull origin main

# 컨테이너 재빌드 및 시작
docker compose up -d --build
```

### 2단계: 대시보드 접속

브라우저에서 아래 주소로 접속:

```
http://서버IP:8080
```

예시:
- `http://13.125.123.456:8080`
- `http://localhost:8080` (서버에서 직접 접속 시)

### 3단계: 상태 확인

대시보드에서 다음 항목을 확인:
- ✅ 컨테이너가 "Running"인지
- ✅ 데이터베이스 연결이 정상인지
- ✅ 크롤링 통계가 표시되는지

---

## 🔧 로컬 개발 환경에서 시작하기

### 1단계: 로컬 환경 시작

```bash
cd /media/ubuntu/data120g/college-crawler

# 로컬 환경 시작 (PostgreSQL 포함)
docker compose -f docker-compose-local.yml up -d

# 로그 확인
docker compose -f docker-compose-local.yml logs -f monitor-local
```

### 2단계: 대시보드 접속

```
http://localhost:8080
```

---

## 📋 빠른 체크리스트

### 서비스가 정상 작동하는지 확인

```bash
# 1. 컨테이너 실행 확인
docker ps | grep monitor

# 예상 출력:
# college-crawler-monitor   Up 5 minutes   0.0.0.0:8080->8080/tcp

# 2. 헬스체크
curl http://localhost:8080/api/health

# 예상 출력:
# {"status":"healthy","timestamp":"2026-02-10T18:45:32"}

# 3. 전체 상태 조회
curl http://localhost:8080/api/status | jq '.'
```

### 문제 발생 시

```bash
# 로그 확인
docker compose logs monitor

# 컨테이너 재시작
docker compose restart monitor

# 포트 확인
netstat -tuln | grep 8080
```

---

## 🌐 포트 8080이 막혀있는 경우

### 방화벽 열기

```bash
# UFW 사용 시
sudo ufw allow 8080/tcp
sudo ufw reload

# iptables 사용 시
sudo iptables -A INPUT -p tcp --dport 8080 -j ACCEPT
sudo iptables-save
```

### 다른 포트 사용하기

`docker-compose.yml` 수정:

```yaml
services:
  monitor:
    ports:
      - "9090:8080"  # 9090으로 변경
```

재시작:

```bash
docker compose up -d --force-recreate monitor
```

접속:
```
http://서버IP:9090
```

---

## 📊 대시보드 미리보기

접속 성공 시 다음과 같은 화면이 표시됩니다:

```
┌─────────────────────────────────────────────────┐
│  🕷️ College Crawler Monitor                     │
├─────────────────────────────────────────────────┤
│                                                  │
│  [✅ Running]  [💾 60개]  [⚡ 2.5%]  [🧠 245MB]  │
│                                                  │
│  📊 크롤링 통계: 성공 23 / 실패 2 (92%)         │
│                                                  │
│  🎓 최근 업데이트된 학교                         │
│  - Los Angeles Trade-Technical College          │
│  - Santa Monica College                          │
│  ...                                             │
│                                                  │
│  📝 최근 로그                                     │
│  [터미널 스타일 로그 출력]                       │
└─────────────────────────────────────────────────┘
```

---

## 🔗 다음 단계

- [📊 모니터링 대시보드 상세 가이드](./MONITORING_DASHBOARD.md)
- [🔍 운영 서버 모니터링](./PRODUCTION_MONITORING.md)
- [⚡ CLI 모니터링 가이드](./QUICK_MONITORING.md)

---

## ❓ FAQ

### Q: 대시보드에 접속이 안 돼요
```bash
# 1. 컨테이너 확인
docker ps | grep monitor

# 2. 로그 확인
docker compose logs monitor

# 3. 재시작
docker compose restart monitor
```

### Q: 데이터가 표시되지 않아요
```bash
# API 테스트
curl http://localhost:8080/api/status

# Docker 소켓 권한 확인
docker compose logs monitor | grep -i "docker"
```

### Q: CPU/메모리 정보가 안 나와요
`docker-compose.yml`에 다음이 있는지 확인:
```yaml
volumes:
  - /var/run/docker.sock:/var/run/docker.sock:ro
```

---

**작성일**: 2026-02-10  
**소요 시간**: 1분
